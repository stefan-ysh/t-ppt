# 项目性能优化配置
NODE_ENV=production

# 构建优化
BUILD_ANALYZE=false
BUILD_CACHE=true

# PPT预览配置
PREVIEW_PORT=8080
PREVIEW_HOST=localhost

# 开发环境配置
DEV_SOURCEMAP=true
DEV_HOT_RELOAD=true

# CDN配置（可选）
CDN_ENABLED=false
CDN_BASE_URL=

# 构建并行配置
BUILD_PARALLEL=true
BUILD_THREADS=4